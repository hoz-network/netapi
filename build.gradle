plugins {
    id 'java'
    id 'org.screamingsandals.plugin-builder' version '1.0.67'
}

allprojects {
    group 'net.hoz'
    version '3.0.0-SNAPSHOT'
}

subprojects {
    apply plugin: 'idea'
    apply plugin: 'java'
    apply plugin: 'java-library'
    apply plugin: 'org.screamingsandals.plugin-builder'

    repositories {
        mavenCentral()
        maven {
            url = "https://repo.screamingsandals.org/snapshots/"
        }
    }

    dependencies {
        api group: 'com.google.guava', name: 'guava', version: GUAVA_VERSION
        api group: 'org.slf4j', name: 'slf4j-api', version: SLF4J_VERSION
    }

    java {
        toolchain {
            languageVersion.set(JavaLanguageVersion.of(17))
        }
    }

    configurations.all {
        // Check for updates every build
        resolutionStrategy.cacheChangingModulesFor 0, 'seconds'
    }
}